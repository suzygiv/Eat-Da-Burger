{{!-- 
  <div class="row">
    <div class="col-sm-6">
    <div class="section-heading">
      <h2>Burgers that are not eaten yet!</h2>
      </div>
    <ul>
      {{#each burgers}}
      {{#unless devoured}}
      {{> burgers/burgers-block devoured=true}}
      {{/unless}}
      {{/each}}
    </ul>
</div>
  <div class="col-sm-6">
    <div class="section-heading">
      <h2>Burgers that were devoured!</h2>
    </div>
    <ul>
      {{#each burgers}}
      {{#if devoured}}
      {{> burgers/burgers-block devoured=false}}
      {{/if}}
      {{/each}}
    </ul>
  </div>
</div>
<div class="addburg">
  <h2>Add a Burger</h2>
  <form class="create-form">

    <div class="form-group">
      <label for="ca">Burger Name:</label>
      <input type="text" id="ca" name="name">
    </div>

    <div class="form-group">
      <label for="devoured">devoured?</label><br>
      <input type="radio" name="devoured" value="1" checked> Eaten already!! Burp!<br>
      <input type="radio" name="devoured" value="0"> Ready to be eaten!!
    </div>

    <button type="submit">Add Burger</button>
  </form>
</div> --}}


<h1>Eat Da Burger!</h1>

<h2>Burgers Available</h2>

<ul>
    {{#each burgers}}
    {{#unless devoured}}
    {{> burgers/burgers-block devoured=false}}
    {{/unless}}
    {{/each}}
</ul>

<h2>Burgers Devoured</h2>

<ul>
    {{#each burgers}}
    {{#if devoured}}
    {{> burgers/burgers-block devoured=true}}
    {{/if}}
    {{/each}}
</ul>

<h2>Add a Burger!</h2>
<form class="create-form">

    <div class="form-group">
        <label for="bu">Create a burger:</label>
        <input type="text" id="createBurger" name="name">
    </div>

    {{!-- <div class="form-group">
    <label for="slee">Sleepy?</label><br>
    <input type="radio" name="sleepy" value="1" checked> Sleepy!<br>
    <input type="radio" name="sleepy" value="0"> Awake
  </div> --}}

    <button type="submit" class="addBurgerBtn">Add Burger</button>
</form>